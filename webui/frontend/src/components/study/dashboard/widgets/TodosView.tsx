/**
 * TodosView — renders todos.md content.
 * Stub: will fetch from API in Phase C.
 */
import type { WidgetProps } from '../types'

export function TodosView(_props: WidgetProps) {
  return (
    <div className="text-xs text-slate-500 italic">
      待办事项将在 API 接通后显示
    </div>
  )
}
